# tzap-plugin-keywrap

`tzap-plugin-keywrap` is a companion crate for tzap recipient wrap workflows in
v44 archives.

The crate currently provides a minimal API boundary for profile dispatch and key
lookup without implementing HPKE envelope generation yet.
