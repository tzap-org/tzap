# v43 core traceability

Source specification:

- `specs/tzap-format-revisedv43.md`
- Document version: `0.43 / 2026-05-31.1`

Scope:

- The matrix covers the documented supported `tzap-core` and CLI archive
  workflows.
- Unsupported workflow shapes are acceptable only when they reject with stable
  diagnostics and are documented in `public-docs/`.
- RootAuth authenticator algorithms are mapped in
  [signing-plugin-traceability.md](signing-plugin-traceability.md).

| ID | Spec area | Requirement summary | Status | Implementation | Evidence |
|---|---|---|---|---|---|
| V43-001 | Format revision and fixed structures | v43 archives set `VolumeHeader.volume_format_rev = 43`; readers reject unsupported revisions, bad magic, bad CRC, non-zero reserved fields, and malformed fixed structures before use. | Implemented and tested | `crates/tzap-core/src/wire.rs`; `crates/tzap-core/src/format.rs`; writer header construction | `volume_format_revision_freshness_is_pinned_to_current_revision`; `wire::tests::fixed_structure_magic_matrix_rejects_all_magic_fields`; `volume_header_rejects_mutations`; fuzz `parse_fixed_structures` |
| V43-002 | Algorithm registry | v43 supported archives use zstd-framed compression, AEAD algorithms including `None`, KDF algorithms including `None`, and Reed-Solomon GF(2^16) FEC; unsupported algorithms reject before payload trust. | Implemented and tested | `crates/tzap-core/src/compression.rs`; `crypto.rs`; `fec.rs`; `wire.rs` | `crypto_header_fixed_rejects_unsupported_profile_values`; `aead_round_trips_all_registered_algorithms`; `compresses_and_decompresses_exact_frame`; `gf16_arithmetic_matches_polynomial_examples` |
| V43-003 | KDF and key sources | Raw and Argon2id KDF parameters are bounded, serialized, authenticated, and exposed through explicit CLI key sources; `KdfAlgo::None` is accepted only with `AeadAlgo::None`. | Implemented and tested | `crates/tzap-core/src/crypto.rs`; `crates/tzap-core/src/wire.rs`; `crates/tzap-cli/src/main.rs` | `parses_raw_kdf_params`; `parses_argon2id_kdf_params`; `rejects_argon2id_params_above_reader_caps`; `cli_create_and_verify_with_password_stdin_argon2id`; v43 plaintext CLI smoke tests |
| V43-004 | CryptoHeader extensions | Extension TLVs are bounded, terminated exactly, reject forbidden or duplicate known tags, and keep unknown critical extensions from being silently accepted. | Implemented and tested | `crates/tzap-core/src/wire.rs`; `crates/tzap-core/src/raw_stream_profile.rs` | `crypto_extension_scanner_enforces_terminator_and_caps`; `crypto_extension_semantics_reject_forbidden_duplicate_and_critical`; `raw_content_model_extension_is_critical_and_exact` |
| V43-005 | Payload pipeline | Payload follows tar member groups to zstd frames, envelope packing, padding, optional AEAD, object-local FEC, striping, and split volumes. | Implemented and tested | `crates/tzap-core/src/writer.rs`; `streaming_writer.rs`; `reader.rs`; `compression.rs`; `padding.rs` | `deterministic_round_trip_property_matrix`; `writer_splits_large_payload_across_seekable_envelopes`; `split_member_frames_carry_exact_boundary_flags`; CLI create/list/verify/extract smoke tests |
| V43-006 | Zstd exactness and padding | Metadata and payload frames consume exactly one complete non-skippable frame where required, and suffix padding is canonical. | Implemented and tested | `compression.rs`; `padding.rs`; metadata and reader decode paths | `compression::tests::rejects_trailing_concatenated_and_skippable_frames`; `padding::tests::rejects_noncanonical_wide_form_padding_below_255`; `metadata_zstd_exactness_mutation_corpus`; fuzz `parse_compressed_and_padding` |
| V43-007 | BlockRecord framing | BlockRecords validate magic, size, kind, flags, CRC, parity/data last flags, monotonic global ordering, and object extent coverage. | Implemented and tested | `wire.rs`; `reader.rs`; `streaming_volume_distributor.rs` | `block_record_round_trips_and_validates_crc`; `block_record_rejects_reserved_kind_flags_and_parity_last_flag`; `rejects_block_record_at_wrong_stripe_position`; `verify_rejects_non_contiguous_global_envelope_indexes` |
| V43-008 | Multi-volume layout and recovery | Stripes are distributed by global block index, duplicate volume indexes reject, recoverable missing/corrupt shards are repaired within authenticated tolerance, and unrecoverable loss fails. | Implemented and tested | `streaming_volume_distributor.rs`; `writer.rs`; `reader.rs`; `fec.rs` | `fixed_multi_volume_stripes_by_global_block_index`; `opens_complete_multi_volume_archive`; `recovers_from_one_missing_volume_when_parity_allows`; `rejects_missing_volume_when_loss_tolerance_zero_even_with_bitrot_parity`; CLI multi-volume recovery smoke |
| V43-009 | Metadata objects | IndexRoot, IndexShard, directory hints, object extents, shard tables, and counted-table offsets are parsed with bounds, order, and consistency checks. | Implemented and tested | `metadata.rs`; `reader.rs`; `writer.rs` | `parses_valid_empty_index_root`; `index_shard_rejects_non_exact_local_frame_and_envelope_tables`; `directory_hint_validation_requires_exact_global_map`; fuzz `parse_metadata` |
| V43-010 | Directory hints | Directory-hint rows are produced when required, ordered by v43 keys, and authenticated archives missing required hints reject. | Implemented and tested | `writer.rs`; `metadata.rs`; `reader.rs` | `directory_hints_are_required_only_above_v41_threshold`; `writer_builds_directory_hint_rows_for_ancestor_directories`; `rejects_authenticated_archive_missing_required_directory_hints`; `rejects_directory_hint_rows_sorted_by_old_v36_key_only` |
| V43-011 | Tar metadata profile | Current supported profile covers regular files, safe paths, local PAX path/linkpath/size, local GNU long name/link, selected mode/mtime restore, and rejects unsafe or unsupported tar shapes. | Implemented and tested | `tar_model.rs`; `streaming_writer.rs`; `reader.rs`; public docs | `supported_tar_metadata_profile_matrix_matches_buffered_and_streaming_parsers`; `tar_metadata_rejects_unsafe_or_inconsistent_overrides_matrix`; `malformed_pax_record_matrix_rejects_before_metadata_is_trusted`; `pax_size_exceeding_available_group_is_rejected_by_buffered_and_streaming_parsers`; `public_docs_pin_tar_metadata_profile`; `safe_restore_rejects_symlink_parent` |
| V43-012 | Bootstrap sidecar | Single-volume bootstrap sidecars validate header flags, integrity-protected manifest material, IndexRoot records, dictionary records, sparse sections, caps, and archive identity. | Implemented and tested | `wire.rs`; `reader.rs`; `writer.rs`; CLI bootstrap paths | `bootstrap_sidecar_header_validates_presence_and_layout`; `bootstrap_sidecar_opens_lists_verifies_and_extracts`; `dictionary_archive_bootstraps_from_sidecar_for_non_seekable_open`; `sidecar_size_cap_rejects_sparse_section_above_class_max` |
| V43-013 | v43 terminal layout | Readers apply the inherited terminal layout, not v36 EOF placement, and validate `VolumeTrailer.bytes_written`, root-auth pointer fields, manifest/trailer adjacency, CMRA, and locator placement. | Implemented and tested | `reader.rs` `locate_v41_terminal*`, `validate_v41_trailer_equations`; `writer.rs` terminal emission | `rejects_authenticated_trailer_pointer_and_count_mutations`; `rejects_authenticated_trailer_outside_trailing_scan_cap`; `sequential_extract_rejects_bytes_after_terminal_locator`; v36 corpus terminal corruption cases |
| V43-014 | CMRA image and recovery | CMRA image, regions, digests, source-authority checks, shard CRCs, FEC repair, caps, locator mirror, and final locator bootstrap critical metadata before trust. | Implemented and tested | `writer.rs` `build_v41_cmra`; `reader.rs` `locate_v41_terminal*`; `wire.rs` terminal structures | `locator_based_cmra_recovery_only_ignores_header_crc_failures`; `key_holding_rejects_cmra_below_authenticated_parity_floor`; `corrupt_v41_terminal_recovery` tests; fuzz `parse_fixed_structures` magic markers |
| V43-015 | Resource caps | Reader and writer enforce active caps for CryptoHeader, KDF, metadata, paths, terminal tail, CMRA, sidecars, shard counts, and extraction output. | Implemented and tested | `metadata.rs`; `reader.rs`; `writer.rs`; `crypto.rs` | `default_reader_caps_match_v41`; `writer_options_reject_reader_resource_cap_excesses`; `live_non_seekable_verify_rejects_terminal_tail_above_cap`; `extraction_rejects_logical_payload_above_total_size_cap` |
| V43-016 | RootAuth core carriage | RootAuth footer wire format, trailer fields, replicated footer equality, descriptor digest, signer identity digest, and root-auth absence/presence rules are enforced by core. | Implemented and tested | `wire.rs`; `root_auth.rs`; `writer.rs`; `reader.rs` | `root_auth_footer_round_trips_and_validates_crc_and_lengths`; `root_auth_writer_config_rejects_reader_cap_excess_before_authenticator`; `unsigned_archive_reports_root_auth_absent`; `root_auth_archive_round_trips_and_verifies_with_callback` |
| V43-017 | Archive root computation | Archive root binds fixed v43 constants, critical metadata, crypto header pre-HMAC or pre-digest bytes, manifest footer global pre-HMAC/pre-digest bytes, index digest, FEC layout digest, data-block Merkle root, signer identity, and descriptor fields. | Implemented and tested | `root_auth.rs`; `reader.rs`; `writer.rs` | `written_archive_authenticates_final_index_root_fec_class`; `root_auth_verification_requires_authenticator_success`; `verify_rejects_authenticated_content_hash_mismatch`; `public_no_key_verifies_encrypted_data_block_commitment_with_callback` |
| V43-018 | Key-holding RootAuth verification | Key-holding RootAuth verification runs only after full archive content conformance and authenticator success; repaired missing volumes can still verify when allowed by authenticated FEC. | Implemented and tested | `OpenedArchive::verify_root_auth_with_verified_content`; `reader.rs` verification helpers | `root_auth_verifies_key_holding_and_public_no_key_modes`; `root_auth_verifies_with_tolerated_missing_volume_after_fec_repair`; `verify_helper_rejects_file_extent_gaps_and_overlaps` |
| V43-019 | Public no-key verification | Public no-key mode validates public-safe header/profile structure, CMRA/RootAuth carriage, CRC-valid data-block observation, archive-root commitment, and diagnostic scope without decrypting metadata. | Implemented and tested | `reader.rs` `public_no_key_verify_*`; CLI public-no-key path | `public_no_key_rejects_unsigned_archives`; `public_no_key_ignores_untrusted_manifest_and_trailer_block_count_fields`; `public_no_key_compares_only_public_crypto_profile_across_volumes`; CLI public no-key tests |
| V43-020 | Public no-key diagnostics | Successful public no-key verification reports data-block commitment verified, physical completeness unverified, and recovery margin unchecked. | Implemented and tested | `PublicNoKeyDiagnostic`; CLI JSON/stdout emitters | `cli_verify_json_success_reports_machine_readable_summary`; `cli_create_signed_archive_and_verify_root_auth_profiles`; public docs RootAuth section |
| V43-021 | Non-seekable and sequential readers | Sequential extraction treats output as provisional, commits only after terminal verification, enforces terminal-tail caps, and rejects root-auth downgrade when retained state is insufficient. | Implemented and tested | `non_seekable_reader.rs`; `reader.rs` sequential paths; `streaming_writer.rs` | `live_non_seekable_extract_stream_commits_after_terminal_verify`; `live_non_seekable_extract_stream_terminal_failure_leaves_no_final_output`; `live_non_seekable_verify_reports_root_auth_wire_only`; `sequential_rejects_when_terminal_authentication_fails_without_returning_bytes` |
| V43-022 | Raw stream profile boundary | `raw_stream_v1` metadata can be parsed as an extension model, but base v43 tar readers reject it before tar metadata trust. | Unsupported and documented | `raw_stream_profile.rs`; CLI stdin mode validation; public docs | `base_v41_readers_reject_raw_stream_profile_before_tar_metadata`; `raw_stream_profile_rejects_before_tar_metadata_poison_corpus`; `cli_create_raw_stdin_unknown_no_spool_returns_profile_blocker`; `public-docs/tzap-operational-boundaries.md` raw stdin section |
| V43-023 | CLI unsupported workflow boundaries | Archive stdout, sidecar stdout, multi-volume bootstrap, selected-path archive stdin extraction, and incompatible stdin options reject with stable unsupported-feature diagnostics. | Unsupported and documented | `crates/tzap-cli/src/main.rs`; `public-docs/tzap-operational-boundaries.md`; `public-docs/tzap-cli-reference.md` | `public_docs_keep_boundaries_out_of_readme_marketing`; `cli_reports_unsupported_feature_with_stable_category_and_exit_code`; `cli_open_commands_reject_multi_volume_bootstrap_before_archive_reads` |
| V43-024 | Error labels and exit categories | Wrong key, corruption, unsupported revision, unsafe extraction, missing bootstrap, and unsupported features have stable CLI labels and exit codes. | Implemented and tested | `crates/tzap-cli/src/main.rs`; CLI error mapping | `cli_reports_wrong_key_with_stable_category_and_exit_code`; `cli_reports_corrupt_archive_after_header_authentication_succeeds`; `cli_reports_unsupported_revision_with_stable_category_and_exit_code`; public-docs tests |
| V43-025 | Fuzz parser coverage | Deterministic fuzz-smoke exercises fixed structures, metadata, compression, padding, and terminal/RootAuth/CMRA parser markers without adding `libfuzzer-sys` to normal workspace tests. | Implemented and tested | `fuzz/fuzz_targets/*`; `fuzz/corpus/*`; `fuzz/corpus/manifest.tsv` | `cargo run --manifest-path fuzz/Cargo.toml --bin fuzz_smoke --locked`; manifest maps parser targets to seed purpose |
| V43-026 | Package and release documentation | Public package docs link the v43 spec, keep private/internal docs out of package READMEs, and keep operational boundaries outside marketing README prose. | Implemented and tested | `README.md`; crate READMEs; `public-docs/`; release tests | `crates_io_metadata::package_readmes_render_without_workspace_paths`; `public_docs_keep_boundaries_out_of_readme_marketing`; `release_workflows` tests |
| V43-027 | Explicit plaintext mode | `--no-encryption` writes v43 archives with `AeadAlgo::None`, `KdfAlgo::None`, zero object tag length, plaintext object framing, no HKDF subkey schedule, and unkeyed fixed-metadata digests; list, extract, and verify open those archives with no archive key while encrypted zero-key archives still require an explicit key source. | Implemented and tested | `crates/tzap-core/src/format.rs`; `crypto.rs`; `wire.rs`; `writer.rs`; `reader.rs`; `non_seekable_reader.rs`; `crates/tzap-cli/src/main.rs` | `cli_extract_reads_unencrypted_archive_without_key_source`; `cli_list_reads_unencrypted_archive_without_key_source`; `cli_verify_reads_unencrypted_archive_without_key_source`; `cli_unencrypted_archive_stdin_reads_without_key_source`; `cli_no_key_does_not_open_encrypted_zero_key_archive`; `cli_encrypted_zero_key_archive_stdin_without_key_is_rejected`; `cli_plaintext_header_digest_corruption_is_corrupt_archive_not_wrong_key` |

## Current matrix result

No implementation gap is recorded for the documented supported workflows in this
pass. Rows marked `Unsupported and documented` are intentionally outside the
current CLI/core surface and are covered by stable rejection behavior plus
`public-docs/` guidance.

The broader phrase "complete v43 compliance" still requires an external
conformance definition for optional or future profiles that the project does
not currently support.
